import { createAction } from '@ngrx/store';

export const showNavbarDashBoardEnable = createAction(
  'showNavbarDashBoardEnable'
);
export const showNavbarDashBoardDisable = createAction(
  'showNavbarDashBoardDisable'
);
